const awsSSM = require('aws-sdk/clients/ssm');
const ssm = new awsSSM();

const getParameter = (parameterName, decrypt = false) => {
    return ssm.getParameter({
        Name: parameterName,
        WithDecryption: decrypt,
    }).promise();
};

module.exports = {
    getParameter,
}