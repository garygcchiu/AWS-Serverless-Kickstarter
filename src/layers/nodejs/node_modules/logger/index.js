const { createLogger, format, transports } = require('winston');

module.exports = createLogger({
    levels: {
        request: 1,
        info: 2,
        warn: 3,
        error: 4,
    },
    level: 'info',
    exitOnError: true,
    format: format.combine(
        format.timestamp(),
        format.splat(),
        format.json()
    ),
    transports: [
        new transports.Console({ silent: process.env.Env === 'test' }),
    ],
});